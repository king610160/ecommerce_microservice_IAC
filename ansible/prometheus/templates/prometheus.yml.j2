global:
  scrape_interval: {{ scrape_interval }}
  evaluation_interval: {{ scrape_interval }}

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
        labels:
          app: "prometheus"